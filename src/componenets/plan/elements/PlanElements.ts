import { styled } from '@mui/material/styles';
import Card from '@mui/material/Card';

export const PlanWidgetCard = styled(Card)({
    marginBottom: '12px',
    padding: '10px',
    flexDirection: 'column',
    display: 'flow',
    minHeight: '50px',
    height: 'auto',
    boxSizing: 'border-box',
    textAlign: 'center',
    borderRadius: '0px',
});
